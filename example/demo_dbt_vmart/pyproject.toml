[project]
name = "demo-dbt-vmart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-vertica",
]

[tool.uv.sources]
dbt-vertica = { path = "../../" }
