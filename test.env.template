# Test Environment Variables Template
# Copy this file to test.env and fill in with your actual test user names
# Note: test.env is ignored by git to keep credentials safe

# Vertica test users for grants testing
# These users must exist in your Vertica database before running grant tests
# Create them with: CREATE USER <username>;
DBT_TEST_USER_1=
DBT_TEST_USER_2=
DBT_TEST_USER_3=
